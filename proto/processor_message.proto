syntax = "proto3";

package processor_message;

option go_package = "./";

message CPU {
    // Brand of the CPU
    string brand = 1;
    // Name of the CPU
    string name = 2;
    uint32 number_cores = 3;
    uint32 number_threads = 4;
    float min_ghz = 5;
    double max_ghz = 6;
}